import { describe, it } from "vitest";

describe("heuristics", () => {
  it.todo("manhattanDistance returns 0 for solved state");
  it.todo("misplacedTiles counts non-blank tiles out of position");
});
