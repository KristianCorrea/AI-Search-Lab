import type { PuzzleState } from "@/modules/puzzle/types/puzzle";

/** Returns true if the puzzle configuration can reach the solved state. */
export function isSolvable(state: PuzzleState): boolean {
  throw new Error("Not implemented");
}
