import { describe, it } from "vitest";

describe("winnerDetection", () => {
  it.todo("findWinner detects row, column, and diagonal wins");
  it.todo("getGameStatus returns draw when board is full");
});
