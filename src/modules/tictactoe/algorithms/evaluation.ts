import type { Board, Player } from "@/modules/tictactoe/types/tictactoe";

/** Evaluates a terminal or non-terminal board from the given player's perspective. */
export function evaluateBoard(_board: Board, _player: Player): number {
  throw new Error("Not implemented");
}
